#!/usr/bin/env bash

title=`playerctl metadata --format "{{ title }}, by {{ artist }} ({{ album }})"`
if [ -z "$title" ]; then
    echo "No music playing"
else
    status=`playerctl status`
    echo "$status: $title"
fi
